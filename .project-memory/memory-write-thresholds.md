# Memory Write Thresholds

## 默认写入门槛
只有会稳定影响后续协作的偏好、项目状态、结构判断或可复用资产，才写入 memory。

## 默认不写入
一次性聊天内容、临时判断、未核实事实、私人敏感信息、只服务当前轮的素材，默认不写入。

## 四类信息的专门门槛
- 稳定偏好：至少能影响多次输出。
- 项目记忆：与项目长期目标或材料范围有关。
- 过程记忆：只在当前任务跨多轮时保留。
- 临时上下文：任务结束后不沉淀。

## 写入前最后检查
确认内容是否长期有效、是否可迁移、是否不含私人路径或凭据。
