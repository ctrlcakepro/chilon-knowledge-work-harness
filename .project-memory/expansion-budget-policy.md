# Expansion Budget Policy

## 新增文件预算
新增 memory 文件前，先确认既有文件无法承载该内容。

## 中枢文件预算
`AGENTS.md`、`PROJECT-WORKFLOW.md` 和 `.project-memory/README.md` 保持轻量，只放跨任务稳定规则。

## 新增前强制检查
新增前先检查是否重复、是否可以合并、是否只是一次性补丁。

## 月度深检默认动作
优先合并近义内容、压缩低频内容、更新索引，再考虑新增结构。
