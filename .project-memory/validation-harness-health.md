# Harness Health Validation

## 顶层是否变胖
检查 `AGENTS.md` 和 `PROJECT-WORKFLOW.md` 是否塞入了低频细节。

## 路由是否清晰
检查任务入口、主交付物和回退路径是否能被快速判断。

## memory 是否还干净
检查 memory 是否包含私人路径、一次性内容、重复规则或过期状态。

## spec 是否真的有用
检查规范文件是否能改变输出质量，而不是只增加阅读负担。
